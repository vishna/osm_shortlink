name: osm_shortlink
description: A Dart implementation of the OpenStreetMap Shortlink algorithm for encoding and decoding coordinates and zoom levels.
version: 0.0.2
repository: https://github.com/vishna/osm_shortlink

environment:
  sdk: ^3.0.0

dependencies:
  meta: ^1.10.0

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0
